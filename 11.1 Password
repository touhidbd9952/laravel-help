//make password with encription

    $password = Hash::make(123456789);

//compare db pasword with user password

    $password = $request->password; 
    $dbpass = $register[0]['password'];  
    if (Hash::check($password, $dbpass))
    {
    
    }
 
